using Microsoft.AspNetCore.Hosting;

using Xunit;

namespace Kafka.Mysql.Tests
{


    public class ConnectionKafka
    {
        //private IHost Host { get; }
        ////private ICacheMySql CacheMySql { get;  }
        //private readonly ICacheMySql _cacheMySql;


        //public ConnectionKafka(BaseFixture<TestStartup> fixture)
        //{
        //    Host = fixture.Hostx;

        //    _cacheMySql = Host.Services.GetRequiredService<ICacheMySql>();
        //}

        //[Fact]
        //public void Test()
        //{
        //    bool fineshed;
        //    do
        //    {
        //        _cacheMySql.Consume(true, out fineshed, CancellationToken.None);
        //    } while (fineshed == false);

        //    Assert.True(fineshed);
        //}

        public ConnectionKafka()
        {
          //  var server = new TestServer(new WebHostBuilder()
                
        }
    }
}
